Simple HTTP Pub/Sub server base on thttpd

                    version 1.2, based on thttpd version 2.25b of 29dec2003

This fork of thttpd runs both as a web server and as a convenient HTTP Pub/Sub bus.
